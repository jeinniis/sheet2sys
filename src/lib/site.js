// DotStack has exactly one author, by design (CONTEXT.md) — a constant
// here instead of a per-entry frontmatter field, so the byline can't drift
// or typo across files. See docs/architecture.md §2 for the reasoning.
export const AUTHOR = 'Jane';
